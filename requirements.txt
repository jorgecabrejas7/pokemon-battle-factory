# Core RL & ML
torch>=2.0.0
stable-baselines3>=2.0.0
gymnasium>=0.29.0
numpy>=1.24.0

# Data & Utilities
pydantic>=2.0.0
pandas

# Emulator Interaction
# Note: mgba python bindings must be installed manually or via system package manager.
# pip install . (inside mgba build dir) or similar.
mgba

# Testing
pytest
